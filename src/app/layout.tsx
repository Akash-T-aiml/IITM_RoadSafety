import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "SmartRescue AI - Premium Emergency Response Platform",
  description: "Futuristic AI-powered emergency dispatch operations and real-time citizen-ambulance coordination.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col bg-background text-text-primary overflow-x-hidden">
        {children}
      </body>
    </html>
  );
}
